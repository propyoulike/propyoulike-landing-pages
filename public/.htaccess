# BigRock Static Hosting Configuration
# Supports SPA routing, project-level deep links, and OG sharing

Options -MultiViews
RewriteEngine On

###############################################
# 1. Pre-render for social media crawlers
###############################################
RewriteCond %{HTTP_USER_AGENT} (facebookexternalhit|twitterbot|WhatsApp|LinkedInBot|TelegramBot) [NC]
RewriteCond %{REQUEST_URI} ^/projects/([^/]+)/?$
RewriteRule ^projects/([^/]+)/?$ /projects/%1/index.html [L]


###############################################
# 2. SPA fallback – must stay AFTER bot rules
###############################################
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ /index.html [QSA,L]


###############################################
# 3. Cache-control
###############################################
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"

  ExpiresByType text/css "access plus 7 days"
  ExpiresByType application/javascript "access plus 7 days"

  # HTML should NOT be cached (important for SPA updates)
  ExpiresByType text/html "access plus 0 seconds"
</IfModule>


###############################################
# 4. GZIP compression
###############################################
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>
